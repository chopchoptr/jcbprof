var canv = document.getElementById("canv-1");var ctx  = canv.getContext('2d');ctx.width = 300;ctx.height = 455;var points = [];var i = 0;write_elements(ctx);function draw_point(i, x, y, ctx){var circle = new Path2D();circle.arc(x, y, 10, 0, 2 * Math.PI);ctx.fill(circle);ctx.fillStyle="#fff";circle.arc(x, y, 8, 0, 2 * Math.PI);ctx.fill(circle);ctx.fillStyle="#000";if (i < 10)ctx.fillText(i, x-3, y+3);else if (i < 100) ctx.fillText(i, x-5, y+3); else ctx.fillText(i, x-9, y+3);} function getMousePosition(canvas, event, ctx){var table = document.getElementById("point-form"); let i = table.rows.length; while (--i) table.rows[i].classList.remove("active-line"); let rect = canvas.getBoundingClientRect(); let x = event.clientX - rect.left; let y = event.clientY - rect.top; let data = read_list(); var table = document.getElementById("point-form"); let min_index = 0; let min_dist = -1; for (let index = 0; index < data.length; index++){ if (min_dist < 0 || (parseInt(x) - parseInt(data[index].x))**2+(parseInt(y) - parseInt(data[index].y))**2 < min_dist){min_index = data[index].pnt; min_dist = (parseInt(x) -  parseInt(data[index].x))**2+(parseInt(y) - parseInt(data[index].y))**2;}} table.rows[min_index].classList.add("active-line");} function read_list(){ let opt = document.getElementsByName("delete[]"); let points = document.getElementsByName("point[]"); let xpoint = document.getElementsByName("x-coord[]"); let ypoint = document.getElementsByName("y-coord[]"); let desk = document.getElementsByName("desc[]"); let data = []; for (let index = 0; index < points.length; ++index){data.push({"pnt" : points[index].value, "x" : xpoint[index].value, "y" : ypoint[index].value, "desc" : desk[index].value});}return(data);} function write_elements(ctx){ let data = read_list(); i = data.length; for (let index = 0; index < data.length; index++) draw_point(data[index].pnt, parseInt(data[index].x), parseInt(data[index].y), ctx);} canv.addEventListener("mousedown", function(e){getMousePosition(canv, e, ctx);}); 